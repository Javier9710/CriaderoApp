<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.ufps.criaderoapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CriaderoApp">

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->

               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.

        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_estanque">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".HomeActivity" /> -->
        <activity android:name=".EstanqueActivity" />
        <activity android:name=".InsumoActivity" />
        <activity android:name=".ProveedorActivity" />
        <activity android:name=".NotificacionActivity"></activity>
        <activity android:name=".regProveedorActivity" />

    </application>

</manifest>